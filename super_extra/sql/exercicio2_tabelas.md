# ANTIGO CREATE TABLE

```sql
CREATE TABLE cidades(
    id NUMERIC,
    nome VARCHAR(50),
    uf VARCHAR(2)
);

CREATE TABLE enderecos
(
    id NUMERIC PRIMARY KEY,
    cep VARCHAR(10),
    rua VARCHAR(100),
    numero NUMERIC,
    complemento VARCHAR(100),
    bairro VARCHAR(100),
    cida_id NUMERIC,
    CONSTRAINT fk_enderecos_cidades
        FOREIGN KEY (cida_id)
        REFERENCES cidades (id)
);

CREATE TABLE clientes(
    id NUMERIC PRIMARY KEY,
    nome VARCHAR(50),
    email VARCHAR(100),
    cpf VARCHAR(11),
    telefone VARCHAR(11),
    data_cadastro TIMESTAMP,
    ende_id NUMERIC,
    CONSTRAINT fk_clientes_enderecos
        FOREIGN KEY(ende_id)
        REFERENCES enderecos(id)
);

CREATE TABLE produtos (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    preco NUMERIC(10, 2) NOT NULL CHECK (preco >= 0),
    categoria VARCHAR(50) NOT NULL,
    estoque INTEGER NOT NULL CHECK (estoque >= 0)
);
```


# DROP TABLES

```sql
DROP TABLE IF EXISTS clientes CASCADE;
DROP TABLE IF EXISTS enderecos CASCADE;
DROP TABLE IF EXISTS cidades CASCADE;
DROP TABLE IF EXISTS produtos CASCADE;
```

# NOVO CREATE TABLE

```sql
--sem FK
--validação de cidade no Java
CREATE TABLE cidades (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nome VARCHAR(50) NOT NULL,
    uf CHAR(2) NOT NULL
);

CREATE TABLE enderecos (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    cep VARCHAR(8) NOT NULL,
    rua VARCHAR(100) NOT NULL,
    numero INTEGER NOT NULL,
    complemento VARCHAR(100),
    bairro VARCHAR(100) NOT NULL,
    cida_id INTEGER
);

CREATE TABLE clientes (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nome VARCHAR(50) NOT NULL,
    email VARCHAR(100) NOT NULL,
    cpf VARCHAR(11) NOT NULL UNIQUE,
    telefone VARCHAR(11) NOT NULL,
    data_cadastro TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    ende_id INTEGER
);

CREATE TABLE produtos (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    preco NUMERIC(10, 2) NOT NULL CHECK (preco >= 0),
    categoria VARCHAR(50) NOT NULL,
    estoque INTEGER NOT NULL CHECK (estoque >= 0)
);
```

---

## ADICIONANDO ALGUNS REGISTROS PARA TESTE

```sql
INSERT INTO cidades (nome, uf) VALUES
('Pato Branco', 'PR'),
('Curitiba', 'PR'),
('Florianópolis', 'SC'),
('São Paulo', 'SP');

INSERT INTO enderecos (cep, rua, numero, complemento, bairro, cida_id) VALUES
('85501266', 'Rua Iguaçu', 957, 'Apto 202', 'Centro', 1),
('85503000', 'Avenida Tupi', 1500, 'Sala 10', 'Baixada', 1),
('80010000', 'Rua XV de Novembro', 300, '', 'Centro', 2),
('88010001', 'Rua Felipe Schmidt', 450, 'Bloco B', 'Centro', 3);

INSERT INTO clientes (nome, email, cpf, telefone, ende_id) VALUES
('Ana Luísa Dariva', 'ana@email.com', '52998224725', '46999998888', 1),
('Amanda Georg', 'amandaG@email.com', '39053344705', '46988887777', 2),
('Rodrigo', 'rodri@email.com', '11144477735', '41999990000', 3),
('Heloise', 'helo@email.com', '98765432100', '48988880000', 4);

INSERT INTO produtos (nome, preco, categoria, estoque) VALUES
('Mouse Gamer RGB', 149.90, 'Gamer', 25),
('Teclado Mecânico Gamer', 399.90, 'gamer', 15),
('Headset GAMER Pro', 249.90, 'GAMER', 8),
('Monitor 24 Polegadas', 899.90, 'eletronicos', 5),

('Detergente Neutro', 4.99, 'Limpeza', 100),
('Desinfetante Lavanda', 12.50, 'limpeza', 60),
('Álcool 70', 9.90, 'LIMPEZA', 80),

('Caixa Organizadora 30L', 59.90, 'Armazenamento', 20),
('Prateleira Modular', 199.90, 'armazenamento', 12),
('Gaveteiro Plástico', 89.90, 'ARMAZENAMENTO', 7),

('Caderno Universitário', 24.90, 'papelaria', 30),
('Caneta Azul', 2.50, 'papelaria', 200),
('Mochila Executiva', 159.90, 'acessorios', 10);
```